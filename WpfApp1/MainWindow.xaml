<Window x:Class="WpfApp1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        Title="Партнеры" Height="600" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="3*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="2*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,10" HorizontalAlignment="Left" VerticalAlignment="Center" >
            <Button Content="Добавить" Width="100" Margin="0,0,10,0" Click="AddPartner_Click"/>
            <Button Content="Редактировать" Width="100" Margin="0,0,10,0" Click="EditPartner_Click"/>
            <Button Content="Удалить" Width="100" Margin="0,0,10,0" Click="DeletePartner_Click"/>
            <Button Content="Обновить" Width="100" Click="RefreshPartners_Click"/>
        </StackPanel>

        <DataGrid x:Name="PartnersDataGrid" Grid.Row="1" AutoGenerateColumns="False" SelectionMode="Single" SelectionChanged="PartnersDataGrid_SelectionChanged" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding ID}" Width="SizeToHeader" IsReadOnly="True"/>
                <DataGridTextColumn Header="Тип партнера" Binding="{Binding PartnerType}" Width="*"/>
                <DataGridTextColumn Header="Наименование" Binding="{Binding Name}" Width="2*"/>
                <DataGridTextColumn Header="Директор" Binding="{Binding Director}" Width="2*"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="2*"/>
                <DataGridTextColumn Header="Телефон" Binding="{Binding Phone}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <Label Content="История покупок партнера:" Grid.Row="2" Margin="0,10,0,5" FontWeight="Bold" />

        <DataGrid x:Name="PurchaseHistoryDataGrid" Grid.Row="3" AutoGenerateColumns="False" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID продукта" Binding="{Binding ProductID}" Width="SizeToHeader"/>
                <DataGridTextColumn Header="Количество продукции" Binding="{Binding Quantity}" Width="*"/>
                <DataGridTextColumn Header="Дата продажи" Binding="{Binding SaleDate, StringFormat=\{0:yyyy-MM-dd\}}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
